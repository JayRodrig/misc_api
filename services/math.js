const math = {};
math.add = (nOne, nTwo) => nOne + nTwo;
math.subtract = (nOne, nTwo) => nOne - nTwo;
math.divide = (nOne, nTwo) => nOne / nTwo;
math.multiply = (nOne, nTwo) => nOne * nTwo;

module.exports = math;